# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Note {
  id: ID!
  text: String!
  done: Boolean!
  user: User!
}

type User {
  id: ID!
  name: String!
}

type Query {
  notes: [Note!]!
}

input NewNote {
  text: String!
  userId: String!
}

type Mutation {
  createNote(input: NewNote!): Note!
}

type Time {
  unixTime: Int!
  timeStamp: String!
}

type Subscription {
  currentTime: Time!
}
